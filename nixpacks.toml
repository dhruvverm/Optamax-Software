[phases.setup]
aptPkgs = ["python3", "make", "g++", "build-essential"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate && npx next build"]

[start]
cmd = "npx prisma migrate deploy 2>/dev/null; npx next start -H 0.0.0.0 -p ${PORT:-3000}"
